import { atom } from "recoil";

export const modalComment = atom({
  key: "modalComment",
  default: "id",
});

// export const modalBookmark = atom({
//   key: "modalBookmark",
//   default: "id",
// });

export const modalWardComment = atom({
  key: "modalWardComment",
  default: "id",
});

export const modalCountyComment = atom({
  key: "modalCountyComment",
  default: "id",
});

export const modalConstituencyComment = atom({
  key: "modalConstituencyComment",
  default: "id",
});

export const modalChatMessage = atom({
  key: "modalChatMessage",
  default: "id",
});

